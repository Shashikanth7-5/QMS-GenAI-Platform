# ══════════════════════════════════════════════════════════════
# QMS GenAI — Complete Requirements
# Sprint 2 complete + Sprint 3 in progress
# Python 3.13 · Windows
#
# Install command (run in PyCharm terminal):
#   pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org
#
# If any single package fails due to network timeout, install it alone:
#   pip install <package> --trusted-host pypi.org --trusted-host files.pythonhosted.org
# ══════════════════════════════════════════════════════════════

# ── Core Flask ─────────────────────────────────────────────────
flask==3.0.3
flask-login==0.6.3
werkzeug==3.0.3
jinja2==3.1.4
python-dotenv==1.0.1

# ── HTTP client (AI API calls) ─────────────────────────────────
httpx==0.27.2

# ── Document parsing ───────────────────────────────────────────
pdfplumber==0.11.0
openpyxl==3.1.2
python-docx==1.1.0
Pillow==10.4.0

# ── Testing ────────────────────────────────────────────────────
pytest==8.2.2
pytest-flask==1.3.0

# ── Sprint 3 — Database (uncomment when PostgreSQL arrives) ────
# sqlalchemy==2.0.31
# psycopg2-binary==2.9.9

# ── Sprint 3 — Fine-tuning (uncomment when API key available) ──
# openai==1.35.0

# ── Sprint 3 — LangChain (uncomment when activating RAG) ───────
# langchain==0.2.0
# langchain-core==0.2.0
# faiss-cpu==1.8.0