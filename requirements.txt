# ══════════════════════════════════════════════════════════════
# QMS GenAI — Complete Requirements
# Sprint 4 hardening pass (QMS-improvisation-v2)
# Python 3.13 · Windows / Linux (containers)
#
# Install:
#   pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org
#
# If a single package fails, install it alone:
#   pip install <package> --trusted-host pypi.org --trusted-host files.pythonhosted.org
# ══════════════════════════════════════════════════════════════

# ── Core Flask ─────────────────────────────────────────────────
# Version bumps in improvement-v1 close pip-audit advisories:
#   Flask   PYSEC-2026-2151
#   Jinja2  PYSEC-2026-1471/1472/1475
#   Werkzeug PYSEC-2026-2044..2320, 3417
#   dotenv  PYSEC-2026-2270
flask==3.1.3
flask-login==0.6.3
flask-wtf==1.2.1
flask-limiter==3.8.0
werkzeug>=3.1.6,<4.0
jinja2==3.1.6
python-dotenv==1.2.2
itsdangerous>=2.2.0
PyYAML>=6.0.2

# ── HTTP client (AI API calls) ─────────────────────────────────
httpx==0.27.2

# ── Document parsing ───────────────────────────────────────────
# pdfplumber pulls pdfminer-six; the 0.11.7 line ships the fixed
# pdfminer-six (>=20250506) that closes PYSEC-2026-1761/1762.
# pdfplumber 0.11.10 pins pdfminer.six==20260107, which is the first
# release free of PYSEC-2026-1761/1762. Do NOT add an explicit
# pdfminer.six pin here — pdfplumber's == pin conflicts with any
# additional constraint.
pdfplumber==0.11.10
openpyxl==3.1.2
python-docx==1.1.0
# Pillow bumped to 12.x closes ~20 CVE advisories against 10.x.
Pillow==12.3.0
reportlab==4.2.2
celery==5.4.0
chromadb==1.5.9
gunicorn==23.0.0
redis==5.0.8

# ── Testing ────────────────────────────────────────────────────
pytest==9.1.1
pytest-flask==1.3.0
pytest-cov==5.0.0

# ── Validation ─────────────────────────────────────────────────
pydantic>=2.0

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9

# ── Sprint 3 — Fine-tuning (uncomment when API key available) ──
# openai==1.35.0

# ── Sprint 3 — LangChain (uncomment when activating RAG) ───────
# langchain==0.2.0
# langchain-core==0.2.0
# faiss-cpu==1.8.0

# ── Dev / CI tooling (optional at runtime) ─────────────────────
# ruff==0.6.9
# bandit==1.7.9
# pip-audit==2.7.3
