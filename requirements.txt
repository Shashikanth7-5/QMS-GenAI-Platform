# ══════════════════════════════════════════════════════════════
# QMS GenAI — Complete Requirements
# Sprint 4 hardening pass (QMS-improvisation-v2)
# Python 3.13 · Windows / Linux (containers)
#
# Install:
#   pip install -r requirements.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org
#
# If a single package fails, install it alone:
#   pip install <package> --trusted-host pypi.org --trusted-host files.pythonhosted.org
# ══════════════════════════════════════════════════════════════

# ── Core Flask ─────────────────────────────────────────────────
flask==3.0.3
flask-login==0.6.3
flask-wtf==1.2.1
flask-limiter==3.8.0
werkzeug>=3.0.6,<4.0
jinja2==3.1.4
python-dotenv==1.0.1
itsdangerous>=2.2.0
PyYAML>=6.0.2

# ── HTTP client (AI API calls) ─────────────────────────────────
httpx==0.27.2

# ── Agentic LLM orchestration (LangChain LCEL) ─────────────────
langchain-core>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.3
langchain-anthropic>=0.2.0,<0.3
langchain-google-genai>=2.0.0,<3.0

# ── Document parsing ───────────────────────────────────────────
pdfplumber==0.11.0
openpyxl==3.1.2
python-docx==1.1.0
Pillow==10.4.0
reportlab==4.2.2
celery==5.4.0
chromadb==1.5.9
gunicorn==23.0.0
redis==5.0.8

# ── Testing ────────────────────────────────────────────────────
pytest==8.2.2
pytest-flask==1.3.0
pytest-cov==5.0.0

# ── Validation ─────────────────────────────────────────────────
pydantic>=2.0

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.31
alembic==1.13.2
# 2.9.9 has no Python 3.13 wheel, so pip falls back to building from source
# and needs libpq-dev (not in Debian slim). 2.9.10 shipped cp313 wheels.
psycopg2-binary>=2.9.10

# ── Sprint 3 — Fine-tuning (uncomment when API key available) ──
# openai==1.35.0

# ── Dev / CI tooling (optional at runtime) ─────────────────────
# ruff==0.6.9
# bandit==1.7.9
# pip-audit==2.7.3
