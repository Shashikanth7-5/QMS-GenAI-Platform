"""Authentication package for the QMS Flask app.

Public API is exposed via `auth.users` — this file exists only to mark the
directory as a Python package. Import via `from auth.users import ...`.
"""
